/* eslint-disable no-unused-vars */
export enum CPU_FETCHING_STATUS {
  IDLE = 'idle',
  FETCHING = 'fetching',
  FAILED = 'failed'
}

export enum ALERT_STATUS {
  RESOLVED = 'resolved',
  PENDING = 'pending'
}
